import { Router } from "express";
// import { userOnlyAPI } from "../utils/guard";

export let indexRouter = Router();

// indexRouter.get("/index", userOnlyAPI, async (req, res, next) => {
//   try {
//   } catch (error) {
//     next(error);
//   }
// });
